[project]
name = "ai-assist-auth-service"
version = "0.1.0"
description = "Domain-layer bootstrap for AI Assist product auth, tenancy, and OAuth token metadata."
requires-python = ">=3.11"

[tool.unittest]
start-dir = "tests"
